# ttf-rasterizer

Small Rust CLI for terminal font sprite previews and generation.

## Use
- `cargo run -- list`
- `cargo run -- preview --font "JetBrains Mono" --text "SHELL QUEST"`
- `cargo run -- generate --font "JetBrains Mono" --text "SHELL QUEST" --output out/title.txt`
